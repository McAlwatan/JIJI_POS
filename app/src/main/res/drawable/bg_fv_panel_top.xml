<?xml version="1.0" encoding="utf-8"?>
<!-- res/drawable/bg_fv_panel_top.xml — dark elevated panel behind the
     quick-actions row, one step lighter than the fv_surface tray behind
     it so the rounded-top shape still reads. -->
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@color/fv_stroke" />
    <corners
        android:topLeftRadius="24dp"
        android:topRightRadius="24dp"
        android:bottomLeftRadius="0dp"
        android:bottomRightRadius="0dp" />
</shape>